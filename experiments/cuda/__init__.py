"""CUDA experiments that do not replace the official RocketPy environment.

Keep this package initializer free of optional PyTorch imports so the
canonical CPU profiling tools remain usable without a CUDA/RL environment.
"""
